# AUDIT — Heat Map · BR

Экспертный аудит проекта (код, сервер, UX, стандарты) и план работ.
Статусы: ✅ сделано · ⏳ в очереди · 💤 отложено по решению владельца.

Оценка зрелости на момент аудита: общая **6.5/10**, UX 8, безопасность 2→(после P0)6,
поддерживаемость 4, надёжность данных 5.

---

## Решения владельца (scope)
- Логины/пароли вьюеров могут оставаться захардкоженными — **ОК**. 💤
- Требование: **править общие данные на сервере может только владелец**; остальные
  заходят по логину и только смотрят (правки локальны). ✅ (см. S1)
- Требование: **загружаемые файлы не хранятся на GitHub**. ✅ (см. S3)

---

## Безопасность

### S1. Ключ записи был зашит в бандл — 🔴 P0 → ✅ ИСПРАВЛЕНО
Раньше `index.html` содержал `_HM_SERVER_KEY`, доступный всем → любой мог писать в
общие данные. Теперь:
- ключ **убран из бандла**;
- владелец вводит его один раз в «Данные → Доступ к записи»; хранится только в его
  `localStorage['hm_admin_key']`;
- запись (`pushToServer`, `pushDataset`) требует `isAdmin() && SERVER_KEY`;
- сервер по-прежнему проверяет `X-API-Key == API_KEY` (env). Без ключа сервер
  отклоняет чужих (401) — защита реальная, а не только в UI.

**Действие владельца:** задать `API_KEY` в переменных окружения сервера (Render) и
ввести то же значение один раз в своём браузере.

### S2. XSS через загружаемые данные — 🟠 P1 → ✅ ИСПРАВЛЕНО
Добавлен helper `esc()`; все пользовательские значения (имена слоёв/точек,
адреса, часы, коды, названия городов) экранируются перед вставкой в
`innerHTML`/попапы/тултипы/`<option>`. Тосты используют `textContent` (безопасны),
`confirm()` — нативный текст. Встроенные ZONES — хардкод, не пользовательские.

### S3. Загружаемые файлы не должны попадать на GitHub — ✅ ПОДТВЕРЖДЕНО/УСИЛЕНО
Данные и так живут на сервере (dataset/state) и в браузере (localStorage), не в репо.
Усилён `.gitignore` (server/state*.json, .env, *.dataset.json, uploads/, *.tmp).
Проверено: в git нет отслеживаемых файлов данных.

### S4. Пароли вьюеров в бандле — 💤 принято владельцем
Оставляем как лёгкий гейт по решению владельца.

---

## Надёжность данных — 🟠 P1

- **A1. Обработка ошибок парсинга** — ✅ `parseSheet` централизован, try/catch на
  CSV/XLSX/чтение, ошибки → тосты.
- **A2. Индикатор обработки** — ✅ тост «Обработка файла…» перед блокирующим
  парсом (через setTimeout, чтобы успел отрисоваться). Web Worker — 💤 при росте файлов.
- **A3. Подтверждение удаления слоя** — ✅ `confirm()` для heat-слоёв и своих точек.
- **A4. Last-write-wins** между двумя админами одной карты — молчаливый клоббер.
  Предупреждать при расхождении версий. ⏳
- **A5. localStorage-кэш датасета (~2.45 МБ)** близко к лимиту — при переполнении
  тихо отключается (обёрнуто в try/catch). Кандидат на IndexedDB. 💤

---

## Производительность — 🟠 P1

- **P1a. `combined` (1.13 МБ, ~46% payload)** — ✅ теперь выводится на клиенте
  (`baseDemandRecs` = cig+sticks) и **не сохраняется** в базу. Экономия материализуется
  после того, как владелец один раз пересохранит базу («Сохранить базу на сервере»).
- **P1b. XLSX/Papa (~900 КБ)** — ✅ грузятся по требованию (`ensureSheetLibs`) при
  первой загрузке/экспорте, а не на старте.
- **P1c. Нет разделения кода.** ⏳ (решается модуляризацией, см. M1)
- ✅ gzip на сервере, кэш датасета (stale-while-revalidate), keep-warm — сделано.

---

## UX / интерфейс / стандарты — 🟡

- **U1. Тотальный UPPERCASE** снижает читаемость (особ. кириллицы). Рекомендация —
  капс только заголовки/лейблы. 💤 (владелец просил не трогать)
- **U2. Кастомные тумблеры `div.cbx` не доступны с клавиатуры** и без `role`/`aria`
  (WCAG). Перевести на `<button role="switch" aria-checked>`. ⏳
- **U3. Смысл только через цвет** (палитры, tier) — добавить текст/паттерны для
  дальтоников. ⏳
- **U4. Нет favicon/meta description** в `<head>`. ⏳
- ✅ Хорошо: адаптив (мобилка/notch/тач), тосты, пустые состояния, визуальная
  система, сворачиваемые группы, кастомный зум.

---

## Поддерживаемость / процесс — 🟠 P1

- **M1. Один файл ~2200 строк на глобальных переменных** — риск регрессий. Разбить
  на модули. ⏳ (следующий отдельный проход — самый рискованный пункт, делать
  изолированно с проверкой на каждом шаге)
- **M2. Линтер/тесты/CI** — ✅ ESLint (`.eslintrc.json`, `npm run lint`) добавлен.
  Юнит-тесты (`cityOf`/`toRecs`/`runAddrFilter`) — ⏳ завязаны на M1 (нужен экспорт
  чистых функций).
- **M3. Мониторинг ошибок** — ✅ глобальный `error`/`unhandledrejection`-обработчик
  (лог + тост). Полноценный Sentry — ⏳ нужен DSN от владельца.
- ✅ `CONTEXT.md`, дисциплина кэш-версионирования.

---

## Архитектура — 🟡

- ✅ Модель «база на сервере + слои от пользователя», раздельные карты comdep/other,
  `cityOf` с фолбэком, Анализ на загруженных слоях — корректно.
- **AR1. `cig/sticks/combined` — «мёртвый груз»** в payload (не используются в UI).
  Либо задействовать, либо исключить из `/data`. ⏳ (связано с P1a)

---

## План работ (порядок исполнения)

1. ✅ **S1** — ограничить запись только владельцем (ключ вне бандла).
2. ✅ **S3** — гарантировать, что данные не попадают в git.
3. ⏳ **S2** — экранирование XSS.
4. ⏳ **A1–A3** — ошибки парсинга + индикатор + подтверждение удаления.
5. ⏳ **P1a/P1b/AR1** — убрать `combined` из payload, lazy-load XLSX.
6. ⏳ **M1** — модуляризация app.js.
7. ⏳ **M2/M3** — линтер, smoke-тесты, Sentry.
8. ⏳ **U2–U4** — доступность, favicon/meta.

*Обновлять статусы по мере выполнения.*
