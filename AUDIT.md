# AUDIT — Heat Map · BR

Экспертный аудит проекта (код, сервер, UX, стандарты) и план работ.
Статусы: ✅ сделано · ⏳ в очереди · 💤 отложено по решению владельца.

Оценка зрелости на момент аудита: общая **6.5/10**, UX 8, безопасность 2→(после P0)6,
поддерживаемость 4, надёжность данных 5.

---

## Решения владельца (scope)
- Логины/пароли вьюеров могут оставаться захардкоженными — **ОК**. 💤
- Требование: **править общие данные на сервере может только владелец**; остальные
  заходят по логину и только смотрят (правки локальны). ✅ (см. S1)
- Требование: **загружаемые файлы не хранятся на GitHub**. ✅ (см. S3)

---

## Безопасность

### S1. Ключ записи был зашит в бандл — 🔴 P0 → ✅ ИСПРАВЛЕНО
Раньше `index.html` содержал `_HM_SERVER_KEY`, доступный всем → любой мог писать в
общие данные. Теперь:
- ключ **убран из бандла**;
- владелец вводит его один раз в «Данные → Доступ к записи»; хранится только в его
  `localStorage['hm_admin_key']`;
- запись (`pushToServer`, `pushDataset`) требует `isAdmin() && SERVER_KEY`;
- сервер по-прежнему проверяет `X-API-Key == API_KEY` (env). Без ключа сервер
  отклоняет чужих (401) — защита реальная, а не только в UI.

**Действие владельца:** задать `API_KEY` в переменных окружения сервера (Render) и
ввести то же значение один раз в своём браузере.

### S2. XSS через загружаемые данные — 🟠 P1 → ✅ ИСПРАВЛЕНО
Добавлен helper `esc()`; все пользовательские значения (имена слоёв/точек,
адреса, часы, коды, названия городов) экранируются перед вставкой в
`innerHTML`/попапы/тултипы/`<option>`. Тосты используют `textContent` (безопасны),
`confirm()` — нативный текст. Встроенные ZONES — хардкод, не пользовательские.

### S3. Загружаемые файлы не должны попадать на GitHub — ✅ ПОДТВЕРЖДЕНО/УСИЛЕНО
Данные и так живут на сервере (dataset/state) и в браузере (localStorage), не в репо.
Усилён `.gitignore` (server/state*.json, .env, *.dataset.json, uploads/, *.tmp).
Проверено: в git нет отслеживаемых файлов данных.

### S4. Пароли вьюеров в бандле — ✅ ИСПРАВЛЕНО
Логин перенесён на серверный `/auth/login`; роли и scrypt-хеши задаются через
`AUTH_USERS_JSON`, а браузер получает только подписанную HttpOnly-сессию.


### S5. Ключ записи лежал в `render.yaml` — 🔴 P0 → ✅ ИСПРАВЛЕНО (июль 2026)
В репозитории хранилось значение `API_KEY` — то есть ключ, защищающий запись,
был публично доступен всем, у кого есть доступ к репо (и остаётся в его истории).
Теперь `API_KEY`, `SESSION_SECRET`, `AUTH_USERS_JSON` и `ALLOWED_ORIGINS` задаются
только через переменные окружения/дашборд Render.

**Действие владельца (обязательно):** сгенерировать **новый** ключ, задать его в
Render → Environment и ввести то же значение в браузере
(«Данные → Доступ к записи»). Старый ключ считать скомпрометированным.

### S6. Ключ проверялся после разбора тела — 🟠 P1 → ✅ ИСПРАВЛЕНО
`express.json({ limit: '50mb' })` отрабатывал **до** проверки `X-API-Key`: любой
аноним мог заставить сервер принять и распаковать 50 МБ (в т.ч. gzip-бомбу) и
только потом получить 401. Проверка ключа вынесена в middleware перед парсером.
Добавлены `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`,
`Referrer-Policy: no-referrer`, отключён `x-powered-by`.

### S7. Внешние скрипты без SRI — 🟡 ⏳
Leaflet / leaflet.heat / PapaParse / XLSX грузятся с cdnjs без `integrity`.
Риск низкий (cdnjs, фиксированные версии), но добавить хеши стоит — только с
проверкой в живом браузере: неверный хеш полностью ломает приложение.

---

## Надёжность данных — 🟠 P1

- **A1. Обработка ошибок парсинга** — ✅ `parseSheet` централизован, try/catch на
  CSV/XLSX/чтение, ошибки → тосты.
- **A2. Индикатор обработки** — ✅ тост «Обработка файла…» перед блокирующим
  парсом (через setTimeout, чтобы успел отрисоваться). Web Worker — 💤 при росте файлов.
- **A3. Подтверждение удаления слоя** — ✅ `confirm()` для heat-слоёв и своих точек.
- **A4. Last-write-wins** между двумя админами одной карты — молчаливый клоббер.
  Предупреждать при расхождении версий. ⏳
- **A5. localStorage-кэш датасета (~2.45 МБ)** близко к лимиту — при переполнении
  тихо отключается (обёрнуто в try/catch). Кандидат на IndexedDB. 💤

---

## Производительность — 🟠 P1

- **P1a. `combined` (1.13 МБ, ~46% payload)** — ✅ теперь выводится на клиенте
  (`baseDemandRecs` = cig+sticks) и **не сохраняется** в базу. Экономия материализуется
  после того, как владелец один раз пересохранит базу («Сохранить базу на сервере»).
- **P1b. XLSX/Papa (~900 КБ)** — ✅ грузятся по требованию (`ensureSheetLibs`) при
  первой загрузке/экспорте, а не на старте.
- **P1c. Нет разделения кода.** ⏳ (решается модуляризацией, см. M1)
- ✅ gzip на сервере, кэш датасета (stale-while-revalidate), keep-warm — сделано.

---

## UX / интерфейс / стандарты — 🟡

- **U1. Тотальный UPPERCASE** снижает читаемость (особ. кириллицы). Рекомендация —
  капс только заголовки/лейблы. 💤 (владелец просил не трогать)
- **U2. Кастомные тумблеры `div.cbx` не доступны с клавиатуры** — ✅ ИСПРАВЛЕНО:
  `role="switch"`, `tabindex`, `aria-checked`, Пробел/Enter, клик по подписи,
  единый `:focus-visible`, `prefers-reduced-motion`, skip-link.
- **U3. Смысл только через цвет** (палитры, tier) — добавить текст/паттерны для
  дальтоников. ⏳ (частично: в легенде и карточках слоёв есть подписи)
- **U4. Нет favicon/meta description** в `<head>` — ✅ ИСПРАВЛЕНО: инлайновый
  SVG-favicon (был 404 на каждой загрузке), description/theme-color/og,
  `noscript`, `robots: noindex`.
- **U5. Плашка городов уезжала под сайдбар** — ✅ ИСПРАВЛЕНО: центрируется по
  карте, ограничена по ширине и скроллится (проявлялось на KG — девять городов).
- **U6. Кнопка «Соло» во включённом состоянии была нечитаема** (бирюзовый текст
  на бирюзовом фоне: старое правило `.lyr-solo.on` перебивало `.lyr-act.on`) —
  ✅ ИСПРАВЛЕНО.
- **U7. Тумблер видимости стоял слева в одних карточках и справа в других** —
  ✅ ИСПРАВЛЕНО: везде справа.
- ✅ Хорошо: адаптив (мобилка/notch/тач), тосты, пустые состояния, визуальная
  система, сворачиваемые группы, кастомный зум.

---

## Поддерживаемость / процесс — 🟠 P1

- **M1. Один файл ~2200 строк на глобальных переменных** — риск регрессий. Разбить
  на модули. ⏳ (следующий отдельный проход — самый рискованный пункт, делать
  изолированно с проверкой на каждом шаге)
- **M2. Линтер/тесты/CI** — ✅ ESLint + **CI** (`.github/workflows/ci.yml`):
  `node --check` клиента и сервера, ESLint, а на PR — проверка, что вместе с
  `app.js`/`style.css` поднята версия кэша в `index.html`.
  Юнит-тесты (`cityOf`/`toRecs`/`runAddrFilter`) — ⏳ завязаны на M1 (нужен экспорт
  чистых функций).
- **M4. Субагенты** — ✅ `.claude/agents/`: `code-reviewer`, `security-auditor`,
  `frontend-ux`, `performance-optimizer`, `geo-analyst`, `docs-keeper`
  (+ прежний `senior-engineer`).
- **M3. Мониторинг ошибок** — ✅ глобальный `error`/`unhandledrejection`-обработчик
  (лог + тост). Полноценный Sentry — ⏳ нужен DSN от владельца.
- ✅ `CONTEXT.md`, дисциплина кэш-версионирования.

---

## Архитектура — 🟡

- ✅ Модель «база на сервере + слои от пользователя», раздельные карты comdep/other,
  `cityOf` с фолбэком, Анализ на загруженных слоях — корректно.
- **AR1. `cig/sticks/combined` — «мёртвый груз»** в payload (не используются в UI).
  Либо задействовать, либо исключить из `/data`. ⏳ (связано с P1a)

---

---

## Изменения по решению владельца (июль 2026)

- **Раздел «Наши точки · IQOS» удалён.** Два закреплённых слоя (BR / 2nd SE) и
  общий датасет `own` убраны полностью. Теперь наши точки — это все слои
  вкладки «Точки»; от них считаются покрытие в рекомендациях, расстояния в
  адресной программе и метрика по городу. Побочный эффект, который был признан
  желательным: карта KG перестала показывать узбекистанские точки.
  Технически: `ourPts()`/`ourPtsInCity()`, `enrichNd()` + `reenrichAll()` при
  каждом изменении состава точек, `__own__` убран из адресной и из выбора слоя
  в «Данные», boot-скрипт больше не требует поля `own` в базе.

## Корректность (найдено в чекапе, июль 2026)

- **C1. Горячие клавиши вкладок** — ✅ было `1–4` при пяти вкладках: «Город»
  недостижим, «4» открывал «Данные» вместо подписанного. Теперь `1–5`.
- **C2. «По размеру» и стартовый вид** — ✅ обрамляли базовый датасет
  (cig+sticks), который на главной вкладке **не рисуется**: кнопка улетала «в
  никуда». Теперь считаются по видимым слоям (`visiblePts()`); для карт вне UZ
  общие узбекистанские точки в расчёт не берутся — KG по-прежнему открывается
  на Бишкеке.
- **C3. Расстояние без наших точек** — ✅ при пустом `own` (карта KG) `nd`
  равнялось 1e18 и печаталось как «1000000000000000 км»; в Excel уезжало число.
  Теперь «неизвестно» = `—`, в выгрузке пустая ячейка.
- **C4. Адресная программа без ориентиров** — ✅ выдавала «нет точек по
  фильтрам»; теперь прямо сообщает, что не загружены точки-ориентиры.
- **C5. Параметры выгрузки адресной** — ✅ лист «Параметры» был привязан к
  удалённому режиму `__shipment__`, поэтому **никогда** не содержал фильтр по
  объёму и список исключений, хотя они применялись. Мёртвые колонки убраны.
- **C6. Счётчик рекомендаций** — ✅ бейдж на вкладке «Анализ» не сбрасывался,
  когда рекомендаций не осталось.
- **C7. Мёртвый код** — ✅ убраны `baseDemandRecs()` и `CITY_CENTERS`.

## Производительность (июль 2026)

- **P2. Поиск ближайшей точки** — ✅ был O(записей × наших точек) и выполнялся
  при каждой загрузке слоя и **каждой** гидрации состояния. Добавлен грид-индекс
  (`buildPtIndex`/`nearestPt`) с доказуемо тем же результатом (сверено с
  брутфорсом на 8 000 случайных запросов). Замер в браузере, 20 000 запросов:
  30 опорных точек — 67→41 мс, 300 — 356→114 мс, 5 000 — 6802→1493 мс.
- **P3. Локальный спрос `ld/lc` в `enrich()`** — ~570 мс на 20 000 точек. ⏳
  Терпимо (разовая операция), кандидат на оптимизацию при росте данных.

---

## План работ (порядок исполнения)

1. ✅ **S1** — ограничить запись только владельцем (ключ вне бандла).
2. ✅ **S3** — гарантировать, что данные не попадают в git.
3. ✅ **S2** — экранирование XSS.
4. ✅ **A1–A3** — ошибки парсинга + индикатор + подтверждение удаления.
5. ✅ **P1a/P1b** — убрать `combined` из payload, lazy-load XLSX.
6. ✅ **S5–S6** — ключ Render вне репозитория, проверка ключа до парсинга тела.
7. ✅ **U2/U4–U7** — доступность, favicon/meta, дизайн-дефекты.
8. ✅ **C1–C7 / P2** — функциональные баги и индекс поиска.
9. ⏳ **M1** — модуляризация app.js (самый рискованный пункт, отдельным проходом).
10. ⏳ **M3** — Sentry (нужен DSN), **A4** — предупреждение о конфликте версий,
    **S7** — SRI для CDN, **AR1** — судьба `cig/sticks` в `/data`.

*Обновлять статусы по мере выполнения.*
